import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.squarespace-cdn.com", "'https://www.iristechnology.com"],
  },
};

export default nextConfig;
